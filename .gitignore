# ===================================================================
# .gitignore for AI Geospatial Analyst
#
# This file tells Git which files and directories NOT to track.
# ===================================================================

# --- Secrets & Environment Variables ---
# CRITICAL: Never commit API keys, passwords, or other secrets.
# Use a .env file for local development, which is ignored here.
.env
*.env
.env.*

# --- Python Virtual Environments & Cache ---
# These are created locally by each developer and should not be shared.
venv/
.venv/
env/
ENV/
__pycache__/
*.pyc

# --- Python Build & Test Artifacts ---
build/
dist/
*.egg-info/
pip-wheel-metadata/
.pytest_cache/
.coverage
htmlcov/

# --- IDE, Editor, and OS Files ---
# Prevents clutter from different developer setups and operating systems.
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# --- Runtime Data & Application Outputs ---
# These files are GENERATED when the application runs.
# They should not be in version control.

# The SQLite knowledge base database file.
data/knowledge_base/*.db

# The cache directory used by libraries like OSMnx.
data/cache/

# The FAISS index file, which is generated from data.
*.faiss

# Log files generated by the application.
*.log
logs/

# The Celery Beat schedule file.
celerybeat-schedule*

# --- Project-Specific Output Directories ---
benchmark_results/

# --- AI/ML Model Files ---
# Models can be gigabytes. They should be managed via a model hub
# or cloud storage, not Git.
*.pt
*.pth
*.h5
*.onnx
model/
models/
# Ignore old or temporary files
old_files/
democode.py
context.md

# NOTE: The 'data/boundaries' directory containing sample shapefiles
# is intentionally NOT listed here, so that Git WILL track it. This
# ensures new developers get the sample data when they clone the repo.